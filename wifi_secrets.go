package main

// Change these before flashing. This file is ignored by Git, so the password
// is not accidentally committed if this directory becomes a repository.
const (
	wifiSSID     = "YOUR_WIFI_NAME"
	wifiPassword = "YOUR_WIFI_PASSWORD"
)
