package main

// Copy this file to wifi_secrets.go and enter your 2.4 GHz Wi-Fi credentials.
// wifi_secrets.go is ignored by Git so the password is not accidentally added
// to source control.
const (
	wifiSSID     = "YOUR_WIFI_NAME"
	wifiPassword = "YOUR_WIFI_PASSWORD"
)
